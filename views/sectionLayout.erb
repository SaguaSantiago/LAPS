<!DOCTYPE html>
<html>
<head>
  <link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,100..900;1,100..900&display=swap" rel="stylesheet">
  <!-- FLATPICKR CSS -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">
  
<title>LAPS</title>
</head>
<body>
    <style>
      :root {
        --bg-color: #1A2820;
        --bg-color-light: #285239;
        --primary-color-light: #53CF86;
        --primary-color: #4CAF50;
        --logo-color: #1165A1;
      }
      body {
        display: flex;
        flex-direction: column;
        /* 
        justify-content: center; */
        min-height: 100vh;
        /* align-items: center; */
        background: var(--bg-color);
        position: relative;
      }

      * {
        font-family: "Roboto", sans-serif;
        padding:0;
        margin:0;
        box-sizing: border-box;
      }

        .section-header {
            width: 100%;
            height: 10vh;
            padding: 0 40px;
            background: linear-gradient(to right, var(--bg-color) 80%, var(--bg-color-light) 100%);
            display: flex;
            justify-content: space-between;
            align-items: center;
            color: white;
            box-shadow: 0px 4px 10px rgba(0,0,0,0.1);
            z-index: 1000;
        }

        .title-container{
          font-size: 40px;
          display: flex;
          justify-content: space-between;
          align-items: center;
        }
        
        .arrow{
          width: 30px;
          height: 30px;
          margin-right: 20px;
          cursor: pointer;
        }

        .logo {
          --transform-scale-x: 1.25;
          --transform-scale-y: 1.25;
          height: 200px;
        }

        .header-title {
          border: none;
          font-size: 40px;
          background: transparent;
          color: white;
          text-decoration: none;
          display: flex;
          align-items: center;    
        }

    </style>

  <% if @error %>
    <p style="color: red;"><%= @error %></p>
  <% end %>

    <header class="section-header">
      <div class="title-container">
        <a href="/" class="header-title"><img src="/images/arrow-back.svg" alt="back" class= "arrow"></a>
        <h1 class="header-title"><%= @sectionName[:label] %></h1>
      </div>

      <a href="/"><img class="logo" src="/images/Logo-light.svg" alt="wallet-logo"></a>
    </header>

  <%= yield %>

  <script>

</script>
</body>
</html>