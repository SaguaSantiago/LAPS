 <div class="container">
    <div class="left">
      <img src="https://i.pravatar.cc/150?img=12" alt="Foto de perfil" class="profile-image">
      <h1></strong> <%= @current_user.username %></h1>

      <div class="wallet-info">
        <p><strong>DNI:</strong> <%= @current_user.dni %></p>
        <p><strong>Teléfono:</strong> <%= @current_user.phone %></p>
        <p><strong>Email:</strong> <%= @current_user.email %></p>
      </div>

      <a href="/editar" class="editar-btn">Editar</a>
    </div>

    
    <div class="right">
      <div class="acciones-right">
        <form action="/logout" method="POST" style="display:inline;">
            <button type="submit" class="logout-btn">Cerrar sesión</button>
        </form


        <form action="/unsubscribe" method="POST" onsubmit="return confirm('¿Estás seguro que querés darte de baja?');">
            <button type="submit" class="delete-btn">Dar de baja</button>
        </form>

      </div>
    </div>
  </div>

<style> 
.container {
  max-width: 1200px;
  width: 95%;
  margin: 60px auto;
  background-color: #3a6a44;
  border-radius: 12px;
  box-shadow: 0 6px 15px rgba(0, 0, 0, 0.1);
  padding: 50px 60px;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 60px;
}

    .left, .right {
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    .profile-image {
      width: 120px;
      height: 120px;
      border-radius: 50%;
      object-fit: cover;
      margin-bottom: 20px;
      box-shadow: 0 0 8px rgba(0, 0, 0, 0.1);
    }

    h1 {
      font-weight: 700;
      color: #ffffff;
      margin-bottom: 30px;
      text-align: center;
    }

    .wallet-info {
      width: 100%;
    }

    .wallet-info p {
      font-size: 18px;
      color: #fff;
      margin: 12px 0;
      border-bottom: 1px solid #e0e0e0;
      padding-bottom: 8px;
    }

    .wallet-info strong {
      color: #111111;
    }

    .editar-btn,
    .logout-btn,
    .delete-btn {
      text-decoration: none;
      padding: 10px 20px;
      border-radius: 8px;
      font-size: 16px;
      font-weight: 500;
      cursor: pointer;
      border: none;
      transition: background-color 0.3s ease;
      margin-top: 10px;
      width: 100%;
      max-width: 250px;
      text-align: center;
    }

    .editar-btn {
      background-color: #3ad17e;
      color: white;
    }

    .editar-btn:hover {
      background-color: #388e3c;
    }

    .logout-btn {
      background-color: #092111;
      color: white;
    }

    .logout-btn:hover {
      background-color: #1976D2;
    }

    .delete-btn {
      background-color: #f44336;
      color: white;
    }

    .delete-btn:hover {
      background-color: #d32f2f;
    }

    .acciones-right {
     display: flex;
    flex-direction: column; 
    gap: 20px;               
    width: 100%;
    align-items: center;    
    }

.acciones-right a,
.acciones-right form button {
  width: 100%;             
  max-width: 250px;        
}

    form {
      margin-left: 259px;
      width: 100%;
    }
</style>

